/**
 * 选择排序
 * @param a 待排序数组
 * @param n 数组长度
 */
public static void selectSort(int[] a, int n) {
        if(n<=0)
            return;
        for(int i=0;i<n;i++){
            int min=i;
            for(int j=i;j<n;j++){
                if(a[min]>a[j]){
                min=j;
        }
        }
                if(min!=i){
                    temp=a[min];
                    a[min]=a[j];
                    a[j]=a[min];
        }

        }
        }
        }